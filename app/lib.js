export const getTheNamesFromList = (list) => {
  return list.map((person) => person.name);
};

export const getAllStudentNames = (grades) => grades.map((grade) => grade);
return grades.name.map((grade) => grades.name);
