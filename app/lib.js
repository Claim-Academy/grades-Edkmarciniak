export const getAllStudentNames = (grades) => grades.map((grade) => grade);

export const getStudentById = (grades) => grades.map((grade) => grade);

export const getStudentGradeById = (grades) => grades.map((grade) => grade);
